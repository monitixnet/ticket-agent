# Repository Working Rules

- Do not create Git commits or push to any remote. The repository owner performs those actions.
- The repository owner must review and explicitly approve code changes before they are committed or merged into `main`.
- Do not deploy or otherwise change production state unless the repository owner gives explicit, current approval for that exact action.
